<p style="color: green"><%= notice %></p>

<h1>Authors</h1>
<%= render partial:'layouts/search_box', locals: {path: authors_path, frame_name: 'authors'} %>
<div id="authors">
  <% @authors.each do |author| %>
    <%= render author %>
    <p>
      <%= link_to "Show this author", author %>
    </p>
  <% end %>
</div>

<%= link_to "New author", new_author_path %>
