json.extract! borrow_extension, :id, :Borrow_id, :ExtensionReason_id, :approver_id, :extended_days, :new_return_date, :cost, :created_at, :updated_at
json.url borrow_extension_url(borrow_extension, format: :json)
